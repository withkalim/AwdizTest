import { createSlice } from '@reduxjs/toolkit';

// const chackToLocalstorage = localStorage.getItem("form") || []; 
const formSlice = createSlice({
  name: "form",
  initialState: { },

  reducers: {

  },
});
